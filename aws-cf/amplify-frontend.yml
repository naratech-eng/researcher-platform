AWSTemplateFormatVersion: 2010-09-09
Description: Amplify Hosting for Next.js frontend with GitHub CI/CD, main/dev branches, and custom domain mapping.

Parameters:
  AppName:
    Type: String
    Default: ResearcherPlatformFrontend
  RepoOwner:
    Type: String
    Description: GitHub organization or user that owns the repository
  RepoName:
    Type: String
    Description: GitHub repository name
  PlatformType:
    Type: String
    AllowedValues: [WEB, WEB_DYNAMIC, WEB_COMPUTE]
    Default: WEB
  NodeVersion:
    Type: String
    Default: "22"
    Description: Node.js major version for build/runtime (e.g., 20 or 22)
  MonorepoAppRoot:
    Type: String
    Default: frontend
  BranchMainName:
    Type: String
    Default: main
  BranchDevName:
    Type: String
    Default: dev
  EnablePRPreviews:
    Type: String
    AllowedValues: ["true", "false"]
    Default: "true"
  DomainName:
    Type: String
    Default: naratech.ca
  ProdSubdomainPrefix:
    Type: String
    Default: app
  DevSubdomainPrefix:
    Type: String
    Default: dev
  GitHubAccessToken:
    Type: String
    NoEcho: true
    Default: ""
    Description: Optional. GitHub Personal Access Token (classic) with repo and admin:repo_hook scopes. Leave blank if using Secrets Manager.
  GitHubAccessTokenSecretArn:
    Type: String
    Default: ""
    Description: Optional. ARN of a Secrets Manager secret whose SecretString is the GitHub PAT. Leave blank to use GitHubAccessToken.

  DevBasicAuthSecretArn:
    Type: String
    Default: ""
    Description: Optional. ARN of Secrets Manager secret with JSON {"username":"...","password":"..."} for dev branch basic auth.
  DevBasicAuthUsername:
    Type: String
    Default: ""
    Description: Optional. Inline username for dev basic auth (used only if DevBasicAuthSecretArn is empty).
  DevBasicAuthPassword:
    Type: String
    NoEcho: true
    Default: ""
    Description: Optional. Inline password for dev basic auth (used only if DevBasicAuthSecretArn is empty).
  UseAmplifyServiceRole:
    Type: String
    AllowedValues: ["true", "false"]
    Default: "true"
    Description: Set to "false" to omit IAMServiceRole from the Amplify App
  UseExistingAppId:
    Type: String
    AllowedValues: ["true", "false"]
    Default: "false"
    Description: Set to "true" to skip creating the Amplify App and use ExistingAppId
  ExistingAppId:
    Type: String
    Default: ""
    Description: When UseExistingAppId=true, provide the existing Amplify AppId

Conditions:
  UseSecret: !Not [!Equals [!Ref GitHubAccessTokenSecretArn, ""]]
  UseInlineToken: !And [!Equals [!Ref GitHubAccessTokenSecretArn, ""], !Not [!Equals [!Ref GitHubAccessToken, ""]]]
  PRPreviewsEnabled: !Equals [!Ref EnablePRPreviews, "true"]
  UseDevBasicAuthSecret: !Not [!Equals [!Ref DevBasicAuthSecretArn, ""]]
  UseDevBasicAuthInline: !And [!Equals [!Ref DevBasicAuthSecretArn, ""], !And [!Not [!Equals [!Ref DevBasicAuthUsername, ""]], !Not [!Equals [!Ref DevBasicAuthPassword, ""]]]]
  UseAmplifyRole: !Equals [!Ref UseAmplifyServiceRole, "true"]
  CreateAmplifyApp: !Equals [!Ref UseExistingAppId, "false"]
  UseExistingApp: !Equals [!Ref UseExistingAppId, "true"]

Resources:
  AmplifyApp:
    Condition: CreateAmplifyApp
    Type: AWS::Amplify::App
    Properties:
      Name: !Ref AppName
      Platform: !Ref PlatformType
      Repository: !Sub "https://github.com/${RepoOwner}/${RepoName}"
      IAMServiceRole: !If [UseAmplifyRole, !GetAtt AmplifyServiceRole.Arn, !Ref "AWS::NoValue"]
      AccessToken: !If
        - UseSecret
        - !Sub "{{resolve:secretsmanager:${GitHubAccessTokenSecretArn}:SecretString}}"
        - !If [UseInlineToken, !Ref GitHubAccessToken, !Ref "AWS::NoValue"]
      EnvironmentVariables:
        - Name: AMPLIFY_MONOREPO_APP_ROOT
          Value: !Ref MonorepoAppRoot
      BuildSpec: !Sub |
        version: 1
        applications:
          - appRoot: ${MonorepoAppRoot}
            frontend:
              phases:
                preBuild:
                  commands:
                    - export NODE_VERSION=${NodeVersion}
                    - echo "Using Node $NODE_VERSION"
                    - corepack enable || true
                    - pwd && ls -la
                    - npm ci
                build:
                  commands:
                    - npm run build
              artifacts:
                baseDirectory: dist
                files:
                  - '**/*'
              cache:
                paths:
                  - node_modules/**/*
            environment:
              variables:
                NODE_VERSION: '${NodeVersion}'

  MainBranch:
    Type: AWS::Amplify::Branch
    Properties:
      AppId: !If [UseExistingApp, !Ref ExistingAppId, !GetAtt AmplifyApp.AppId]
      BranchName: !Ref BranchMainName
      EnableAutoBuild: true
      Stage: PRODUCTION
      EnablePullRequestPreview: !If [PRPreviewsEnabled, true, false]

  DevBranch:
    Type: AWS::Amplify::Branch
    Properties:
      AppId: !If [UseExistingApp, !Ref ExistingAppId, !GetAtt AmplifyApp.AppId]
      BranchName: !Ref BranchDevName
      EnableAutoBuild: true
      Stage: DEVELOPMENT
      EnablePullRequestPreview: !If [PRPreviewsEnabled, true, false]
      BasicAuthConfig: !If
        - UseDevBasicAuthSecret
        - { EnableBasicAuth: true, Username: !Sub "{{resolve:secretsmanager:${DevBasicAuthSecretArn}:SecretString:username}}", Password: !Sub "{{resolve:secretsmanager:${DevBasicAuthSecretArn}:SecretString:password}}" }
        - !If
          - UseDevBasicAuthInline
          - { EnableBasicAuth: true, Username: !Ref DevBasicAuthUsername, Password: !Ref DevBasicAuthPassword }
          - !Ref "AWS::NoValue"

  DomainAssociationProd:
    Type: AWS::Amplify::Domain
    DependsOn:
      - MainBranch
    Properties:
      AppId: !If [UseExistingApp, !Ref ExistingAppId, !GetAtt AmplifyApp.AppId]
      DomainName: !Ref DomainName
      SubDomainSettings:
        - BranchName: !Ref BranchMainName
          Prefix: !Ref ProdSubdomainPrefix

  DomainAssociationDev:
    Type: AWS::Amplify::Domain
    DependsOn:
      - DevBranch
    Properties:
      AppId: !If [UseExistingApp, !Ref ExistingAppId, !GetAtt AmplifyApp.AppId]
      DomainName: !Ref DomainName
      SubDomainSettings:
        - BranchName: !Ref BranchDevName
          Prefix: !Sub "${DevSubdomainPrefix}-${ProdSubdomainPrefix}"

  AmplifyServiceRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: amplify.amazonaws.com
            Action: 'sts:AssumeRole'
          - Effect: Allow
            Principal:
              Service: amplify.us-east-2.amazonaws.com
            Action: 'sts:AssumeRole'
          - Effect: Allow
            Principal:
              Service: amplifybackend.amazonaws.com
            Action: 'sts:AssumeRole'
          - Effect: Allow
            Principal:
              Service: codebuild.amazonaws.com
            Action: 'sts:AssumeRole'
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AdministratorAccess

Outputs:
  AppId:
    Value: !If [UseExistingApp, !Ref ExistingAppId, !GetAtt AmplifyApp.AppId]
  DefaultDomain:
    Condition: CreateAmplifyApp
    Value: !GetAtt AmplifyApp.DefaultDomain
  AmplifyConsoleAppUrl:
    Value: !If
      - UseExistingApp
      - !Sub "https://${AWS::Region}.console.aws.amazon.com/amplify/home?region=${AWS::Region}#/${ExistingAppId}"
      - !Sub "https://${AWS::Region}.console.aws.amazon.com/amplify/home?region=${AWS::Region}#/${AmplifyApp.AppId}"
  DevSubdomain:
    Value: !Sub "${DevSubdomainPrefix}.${ProdSubdomainPrefix}.${DomainName}"
  ProdSubdomain:
    Value: !Sub "${ProdSubdomainPrefix}.${DomainName}"
